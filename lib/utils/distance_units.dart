/// Distance-unit constants shared by UI formatting, statistics, and
/// achievement thresholds. Stored distances are always meters; conversion to
/// the user-selected display unit happens at the presentation boundary.
class DistanceUnits {
  /// Meters in one (statute) mile.
  static const double metersPerMile = 1609.34;
}
